services:
  s3:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: docker-s3-toolkit
    env_file:
      - .env
    volumes:
      - ./data:/data
      - ./scripts:/scripts
    # Uncomment for bucket mount support (FUSE)
    # cap_add:
    #   - SYS_ADMIN
    # devices:
    #   - /dev/fuse
    # security_opt:
    #   - apparmor:unconfined
